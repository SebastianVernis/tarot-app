# Optimized requirements for Vercel deployment
# Total size: ~50-80 MB (well under 250 MB limit)

# Flask and extensions (lightweight)
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-CORS==4.0.0
Flask-Migrate==4.0.5

# Database
SQLAlchemy==2.0.23

# Security
Werkzeug==3.0.1
PyJWT==2.8.0

# Utilities
python-dotenv==1.0.0

# Timezone support (lightweight alternative)
pytz==2023.3

# Gemini AI
google-generativeai==0.3.2

# REMOVED HEAVY DEPENDENCIES:
# - numpy (50-80 MB) - not needed for basic tarot readings
# - matplotlib (100-150 MB) - not needed for API
# - scipy (80-100 MB) - not needed for basic functionality
# - pyswisseph (20-30 MB) - astrology features will be optional/disabled
# - timezonefinder (large data files) - use pytz only

# Note: Astrology features requiring pyswisseph will be disabled
# or moved to a separate microservice if needed

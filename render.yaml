services:
  # Tarot App - Static Site
  - type: web
    name: tarot-reader
    runtime: static
    plan: free
    branch: master
    buildCommand: echo "No build needed - static files only"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /
        destination: /tarot_web.html
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
